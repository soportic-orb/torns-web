@php Theme::layout('full-width'); @endphp

<div class="mx-auto max-w-3xl px-4 py-16 sm:px-6">
    <h1 class="font-display text-2xl font-bold">{{ __('Homepage not configured yet') }}</h1>
    <p class="mt-4 text-torns-ink/70">
        {{ __('Create a page with the homepage sections and select it under Appearance → Theme options → Page.') }}
    </p>
</div>
